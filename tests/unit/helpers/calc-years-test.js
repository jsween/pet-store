import { calcYears } from 'e-commerce/helpers/calc-years';
import { module, test } from 'qunit';

module('Unit | Helper | calc years');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = calcYears([42]);
  assert.ok(result);
});
