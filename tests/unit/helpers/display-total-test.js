import { displayTotal } from 'e-commerce/helpers/display-total';
import { module, test } from 'qunit';

module('Unit | Helper | display total');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = displayTotal([42]);
  assert.ok(result);
});
